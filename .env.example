# 이 파일을 .env로 복사하고 실제 값 채워넣기
PORT=4000

DB_HOST=localhost
DB_PORT=3306
DB_NAME=poke_stone
DB_USER=poke_stone_user
DB_PASSWORD=여기에_MySQL_비밀번호

# 아무 문자열이나 길고 무작위하게. 예: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=여기에_랜덤한_긴_문자열
